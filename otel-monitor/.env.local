# Gmail SMTP Configuration
# Get App Password from: https://myaccount.google.com/ → Security → App passwords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=samplealertsemail@gmail.com                    # Your Gmail address
SMTP_PASS=dfkx vprt dzfw bgzj                     # Google App Password (16 chars)

# Alert Recipients
ALERT_EMAIL_TO=samplealertsemail@gmail.com               # Where alerts go

# Service URLs
PROM_URL=http://localhost:9090                    # Prometheus
GRAFANA_URL=http://localhost:3001                 # Dashboard (otel-monitor)
